# Focus window to the left
alt + Left
    bspc node -f west.local.!hidden.window

# Focus window to the right
alt + Right
    bspc node -f east.local.!hidden.window

# Focus window above
alt + Up
    bspc node -f north.local.!hidden.window

# Focus window below
alt + Down
    bspc node -f south.local.!hidden.window
